psycopg2-binary # PostgreSQL driver
pymongo # Only for the one-time Mongo->Postgres cutover (migrations/mongo_to_postgres.py).
        # Nothing in v1/ imports it. Remove once the cutover is done — see docs/postgres-cutover.md
requests # HTTP requests
PyJWT # JWT tokens
pytest # Testing
requests-mock # Mocking HTTP requests
cryptography # For Fernet encryption
pytz # Timezone support
fastapi # FastAPI framework
fastapi-health # Health checks for FastAPI
python-multipart # Multipart form data
uvicorn # ASGI server for FastAPI
sqlalchemy # ORM for databases
alembic # Database migrations
faker # Fake data generator
googlemaps # Google Maps API
apscheduler # Scheduling periodic tasks

# Documentation
sphinx
sphinx-markdown-builder